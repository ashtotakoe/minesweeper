export const gameElementTextNodes: Record<'base' | 'plate' | 'bento' | 'apple', string[]> = {
  base: ['<div class="table">', '</div>'],
  plate: ['<plate', '</plate>'],
  bento: ['<bento', '</bento>'],
  apple: ['<apple', '</apple>'],
}
